# Rust
/target/

# Node / frontend
node_modules/
dist/
.vite/

# Tauri
src-tauri/target/
# Regenerated by tauri-build on every compile from tauri.conf.json and
# capabilities/ — build output, not source.
crates/jig-app/gen/schemas/

# jig check writes its HTML report card to the cwd by default
jig-report-*.html

# Census harness working files: raw npm search dumps (regenerate with
# scripts/census2/build-candidates.js --fetch-pools) and run checkpoints.
data/pools/
*.progress.jsonl

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
*.key

# IDE
.idea/
.vscode/
# Added by code-review-graph
.code-review-graph/
inspect-stderr.log
