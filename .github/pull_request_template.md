## What & why

<!-- One or two sentences. What does this change, and what problem does it solve? -->

## Checklist

- [ ] `cargo fmt --all` — formatted
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` — clean
- [ ] `cargo test --workspace` — green
- [ ] Commits are signed off (DCO: `git commit --signoff`)
- [ ] Docs updated, if this changes user-facing behavior

## Notes for the reviewer

<!-- Anything that needs context: a tricky decision, a server that misbehaves,
     a follow-up you deliberately left out. Delete if there's nothing to add. -->
